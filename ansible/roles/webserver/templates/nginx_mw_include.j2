
    location ~* \.php$ {
    fastcgi_pass unix:/run/php/php7.3-fpm.sock;
    include         fastcgi_params;
    fastcgi_param   SCRIPT_FILENAME    $document_root$fastcgi_script_name;
    fastcgi_param   SCRIPT_NAME        $fastcgi_script_name;
  }

    location ~ /\.ht {
        deny all;
    }

# # BEGIN added by ansible
# ## Mediawiki 
#     # sfb1468 wiki - pretty URLs

#     location /sfb1368/wiki/images {
#         # Separate location for images/ so .php execution won't apply
#     }

#     location /sfb1368/wiki/images/deleted {
#         # Deny access to deleted images folder
#         deny all;
#     }

#     # MediaWiki assets (usually images)
#     location ~ ^/sfb1368/wiki/resources/(assets|lib|src) {
#         try_files $uri 404;
#         add_header Cache-Control "public";
#         expires 7d;
#     }

#     # Assets, scripts and styles from skins and extensions
#     location ~ ^/sfb1368/wiki/(skins|extensions)/.+\.(woff2|woff|ttf|css|js|gif|jpg|jpeg|png|svg|wasm)$ {
#         try_files $uri 404;
#         add_header Cache-Control "public";
#         expires 7d;
#     }

#     location /sfb1368/wiki/ {
#         rewrite ^/sfb1368/wiki/(?<pagename>.*)$ /sfb1368/wiki/index.php;
#     }